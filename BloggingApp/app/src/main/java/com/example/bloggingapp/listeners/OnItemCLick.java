package com.example.bloggingapp.listeners;

import android.view.View;
import android.widget.ImageView;

public interface OnItemCLick {
    void onCLick(int position, View view, ImageView imageView);
}
